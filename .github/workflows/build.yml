name: Bootstrap flucidOS Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-bootstrap:
    runs-on: ubuntu-24.04
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Maximize Build Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: Install ORAS & Login
        run: |
          curl -LO "https://github.com/oras-project/oras/releases/download/v1.2.0/oras_1.2.0_linux_amd64.tar.gz"
          mkdir -p oras-install
          tar -zxf oras_1.2.0_linux_amd64.tar.gz -C oras-install
          sudo mv oras-install/oras /usr/local/bin/
          echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Compute cache coordinates
        id: cache-vars
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          SAFE_TAG=$(echo "${{ github.ref_name }}" | tr -c 'a-zA-Z0-9._-' '-')
          echo "repo=ghcr.io/${REPO_LOWER}/bst-cache" >> "$GITHUB_OUTPUT"
          echo "tag=${SAFE_TAG}" >> "$GITHUB_OUTPUT"

      - name: Restore BuildStream caches
        env:
          GHCR_CACHE_REPO: ${{ steps.cache-vars.outputs.repo }}
          CACHE_TAG: ${{ steps.cache-vars.outputs.tag }}
        run: |
          ./scripts/bst-cache.sh restore sources
          ./scripts/bst-cache.sh restore cas

      - name: Pull BuildStream image
        run: docker pull docker.io/buildstream/buildstream:latest

      - name: Track & build adjusted.bst
        run: |
          docker run --privileged --rm \
            -v "$PWD":/repo -w /repo \
            -v "$HOME/bst-cache":/root/.cache/buildstream \
            docker.io/buildstream/buildstream:latest \
            bash -c "bst source track adjusted.bst && bst build adjusted.bst"

      - name: Run toolchain smoke test
        run: |
          docker run --privileged --rm \
            -v "$PWD":/repo -w /repo \
            -v "$HOME/bst-cache":/root/.cache/buildstream \
            docker.io/buildstream/buildstream:latest \
            bst build tools/test.bst

      - name: Check out result
        run: |
          rm -rf result
          docker run --privileged --rm \
            -v "$PWD":/repo -w /repo \
            -v "$HOME/bst-cache":/root/.cache/buildstream \
            docker.io/buildstream/buildstream:latest \
            bst --no-strict artifact checkout -d none --no-integrate adjusted.bst --directory result/

      - name: Upload result artifact
        uses: actions/upload-artifact@v4
        with:
          name: flucidOS-bootstrap-result
          path: result/

      - name: Save BuildStream caches
        if: always()
        env:
          GHCR_CACHE_REPO: ${{ steps.cache-vars.outputs.repo }}
          CACHE_TAG: ${{ steps.cache-vars.outputs.tag }}
        run: |
          ./scripts/bst-cache.sh save sources
          ./scripts/bst-cache.sh save cas
